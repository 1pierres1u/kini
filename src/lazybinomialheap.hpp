#ifndef LAZY_BINOMIAL_HEAP_HPP
#define LAZY_BINOMIAL_HEAP_HPP

class LazyBinomialHeap{

	public:
		LazyBinomialHeap(){}
		void enqueue(int kx){}
		void extract_min(){ }
		int find_min(){
			return 0;
		}

};
#endif
