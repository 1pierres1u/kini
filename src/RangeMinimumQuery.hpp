#ifndef RANGE_MINIMUM_QUERY_HPP
#define RANGE_MINIMUM_QUERY_HPP

#include <vector>

class RangeMinimumQuery{
	public:
		RangeMinimumQuery(const std::vector<int>& V){}
		void build(){}
		int get_minimum(size_t i, size_t j)const{
			return 0;
		}
};
#endif
