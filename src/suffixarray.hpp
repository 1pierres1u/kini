#ifndef SUFFIX_ARRAY_HPP
#define SUFFIX_ARRAY_HPP

#include <string>
#include <vector>
#include <algorithm>

class SuffixArray{
	private:
		std::string s;
		std::vector<size_t> sa;
	public:
		SuffixArray(const std::string& sx):s(s){
		}
		void build(){
		}
};
#endif
